import { HyperlinkSwiperDirective } from './hyperlink-swiper.directive';

describe('HyperlinkSwiperDirective', () => {
  it('should create an instance', () => {
    const directive = new HyperlinkSwiperDirective();
    expect(directive).toBeTruthy();
  });
});
